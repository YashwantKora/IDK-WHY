#include <iostream>
//typedef std::string text_t;
//typedef int number_t;

using number_t = int;
using name_t = std::string;

int main(){
    name_t name = "Snowy";
    std::cout << name << std::endl;

    number_t age = 2;
    std::cout << age;
}

